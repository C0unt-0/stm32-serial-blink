# Legacy examples

These files preserve earlier experimental entry points for reference. They are excluded from both Makefile and CMake production source lists.

- `mainb.cpp`: original minimal application-loop sketch
- `mainwalker.cpp`: original AW9523 channel-walk experiment
